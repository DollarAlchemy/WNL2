# fonts/

Drop self-hosted font files (.woff2) here to avoid the Google Fonts network dependency.

Update `css/base.css` to replace the Google Fonts `<link>` with `@font-face` rules pointing here.

Recommended: download Cinzel and Crimson Pro from Google Fonts → Download Family.
